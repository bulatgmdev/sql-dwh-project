/*
=============================================================
Создание базы данных
=============================================================
Назначение скрипта:
    Этот скрипт создаёт новую базу данных 'datawarehouse',
    предварительно проверяя её существование.
    Если база данных уже существует, она удаляется
    и создаётся заново.

ВНИМАНИЕ:
    Запуск этого скрипта удалит всю базу данных
    'datawarehouse', если она существует.
    Все данные в базе будут безвозвратно удалены.
    Необходимо действовать с осторожностью и убедиться, что есть
    актуальные резервные копии перед запуском этого скрипта.

Как запускать:
    1. Лучший способ: через psql (рекомендуется)
       psql -U ваш_пользователь -d postgres -f 01_create_database.sql
    2. В pgAdmin/DBeaver: необходимо запустить сначала только строку с DROP,
       потом отдельно строку с CREATE (в двух разных запросах).
*/

-- Удаление и повторное создание базы данных 'datawarehouse'
DROP DATABASE IF EXISTS datawarehouse;
CREATE DATABASE datawarehouse;
